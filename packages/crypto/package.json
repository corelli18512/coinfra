{
  "name": "@coinfra/crypto",
  "version": "0.1.0",
  "description": "Modern hybrid public-key encryption (X25519 + HKDF-SHA256 + AES-256-GCM) and Ed25519 challenge-response signing. Multi-recipient, self-describing envelopes. Cross-platform Web Crypto, no dependencies.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "sideEffects": false,
  "scripts": {
    "build": "tsgo",
    "dev": "tsgo --watch",
    "typecheck": "tsgo --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist .turbo"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@typescript/native-preview": "catalog:",
    "vitest": "catalog:"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/corelli18512/coinfra.git",
    "directory": "packages/crypto"
  }
}
